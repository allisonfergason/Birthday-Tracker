//
//  CountdownView.swift
//  Birthday Tracker
//
//  Created by Allison Fergason on 7/29/24.
//

import SwiftUI

struct CountdownView: View {
    var body: some View {
        Text("Birthday Tracker")
        // months and days
        // months on top of days
        // make months disappear when 0
        // if that's too difficult then just do days
        // big number small months/days
        // big number on top of "months" on top of big number on top of "days"
    }
}

#Preview {
    CountdownView()
}
